# TASK 37 — bounded round-2 failure recovery

## Incident and evidence

The operator's second replay stopped after one done case and one error; the remaining cases were not evaluated. Runtime logged ValueError, not a valid decision to ignore.

A single diagnostic replay on the isolated lab at 2026-09-23T09:18Z reproduced the cause without writing results: the classifier chose action=silent / reason=admin_announcement and referenced the CURRENT announcement. validate_plan treated every evidence ID as a reference to past context and raised `unknown evidence id`.

No source text, private identifiers, credentials or real fixtures belong in this repository.

## Bounded correction

- A reference to current is recognized as a real input explaining the classification.
- Current is NEVER used as authoritative evidence for an answer. Only trusted, supplied past context can ground that answer; current-only answer plans downgrade to clarification.
- Silent decisions discard unused evidence references and do not call the generator.
- Genuinely unknown IDs and future context remain errors. API/schema errors are not turned into silence.
- Shared production components, prompts, existing education_community_v1 and live groups remain unchanged.

## Explicit continuation

Owner command `/lab_resume` in the same paired group retries only error/interrupted cases and cases not yet run. Done cases are retained. No new upload or round reset.

Before any retried call, publish a private recovery snapshot of the partial results, original manifest and previous continuation metadata. Original /data artifacts and round01 are untouched. Original round02 manifest is also immutable.

The known deployed source implementation (commit 16bd5af, fingerprint pinned in code) may transition to this repair ONLY through explicit owner recovery. Model changes and unrelated policy versions are refused. Normal run checks now also include git_commit.

A separate continuation.json records the new implementation, original manifest hash, snapshot and exact provenance of retained successful results. New results carry execution_version. The HTML explicitly labels this as a composite continuation report; it must not imply all results were generated by the same implementation.

A new failure stops the batch; subsequent cases remain not_run. Diagnostics contain safe error codes, not exception bodies. Repeated command while busy or after completion never launches duplicate calls.

## Validation and rollout

Synthetic tests cover current references for silence and answers, unknown-source rejection, failed/pending-only retry, baseline/done preservation, provenance, owner isolation, duplicate runs, revision mismatch and safe errors.

Local clone was attempted and failed: Could not resolve host: github.com. Consequently local repository preflight/full pytest are not claimed. Full PostgreSQL tests_v2 must be confirmed through GitHub Actions before deployment.

Deploy only the existing isolated Group Lab service from fix/lab-round2-evidence-recovery. Preserve /data and credentials. Start command remains python -m app_v2.labs.telegram_lab_rounds, removing the temporary diagnostic wrapper. Never merge to live v2/main to deploy this repair. Deployment alone does not run or silently resume historical cases.

## Operator next step after verified deploy

Send `/lab_resume@BROConrol_bot` once in the existing lab group. It starts unfinished cases directly, then returns group_lab_round02.html. Review actual model quality only after the complete report.
